import React, {PropTypes} from 'react';

const Component = () => (
  <div />
);

Component.propTypes = {
  variant: props => {
    const variants = [null];
    return variantſ[0];
  }
};

export default Component;
